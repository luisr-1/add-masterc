[] - Implementar o tipo Big Int
[] - Imlementar a soma de Big Int
[] - Implementar a subtração de Big Int
[] - Implementar a encapsulação do tipo number, incluindo o tipo que irá representar o número, usando funções auxiliares
checando se deve realizar o cálculo usando, vamos definir que n :=> Número:
    * int ~> INT_MIN <= n <= INT_MAX
    * long long int ~> LONG_LONG_MIN <= N <= LONG_LONG_MAX
    * unsigned long long int ~> n > 0 && n <= UNSINGED_LONG_LONG_MAX
    * BigInt ~> n > int, long long, unsigned long long limits.
[] - Nas operações sempre checar se o resultado é elegível para a promoção de tipo BigInt, analisando se é possível não gastar esse recurso.
